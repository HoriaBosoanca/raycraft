# Raycppcraft
(raylib c++ craft) \
This is a small voxel game. WIP

## Todo:

Add content
- ?

Fix physics
- Fix camera teleportation when jumping (happens sometimes)
- Fix getting stuck on ledges
- Fix not being able to jump on the edge of a block

Optimize renderer
- Make renderer just an api and access it (to generate the blocks) from somewhere else
- Check for obfuscated blocks across chunks
- Eventually make the renderer skip all overlapping faces

Rewrite code
- Wrap more things in objects and make proper destructors and shutdown functions